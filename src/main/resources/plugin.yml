name: ChatEmoji
version: '${version}'
main: org.ramki.chatemoji.ChatEmoji
api-version: '1.21'
folia-supported: true
description: Player head emojis in chat.
authors: [ ue_rami ]

dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE #
      required: false

commands:
  emoji:
    usage: /emoji